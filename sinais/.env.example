# Base de dados - usa ficheiro diferente da app original (sinais-vol-rsi)
# Local: SQLite. Railway: usar PostgreSQL (nova instância = base separada)
DATABASE_URL="file:./data/sinais-vol-rsi.db"

# Segredo para cron jobs (cron-job.org, etc.)
CRON_SECRET=seu-token-seguro-aqui

# Código de acesso à app (opcional)
ACCESS_CODE=
