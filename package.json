{
  "name": "bot-cripto",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd sinais && npm ci && npm run build",
    "start": "cd sinais && npm start"
  },
  "engines": {
    "node": ">=18 <22"
  }
}
